import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
// import router from './router'  // 暂时注释掉，等vue-router安装后再启用

createApp(App).mount('#app')
// createApp(App).use(router).mount('#app')  // 暂时注释掉
